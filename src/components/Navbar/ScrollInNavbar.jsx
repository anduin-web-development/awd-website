import React from 'react';
import ScrollIn from './ScrollIn';
import Navbar from './Navbar';

const ScrollInNavbar = () => {
  return (
    <ScrollIn>
      <Navbar />
    </ScrollIn>
  );
};

export default ScrollInNavbar;
